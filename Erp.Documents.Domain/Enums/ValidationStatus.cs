namespace Erp.Documents.Domain.Enums
{
    /// <summary>
    /// Estados de validación para documentos.
    /// P = Pendiente
    /// A = Aprobado
    /// R = Rechazado
    /// </summary>
    public enum ValidationStatus
    {
        P, // Pendiente
        A, // Aprobado
        R  // Rechazado
    }
}
